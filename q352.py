#Assume the variables x and y have been assigned integer values. Write an if statement that assigns True to the status variable if x is non-negative and y is negative.
status = False
if(x >= 0 and y < 0):
    status = True
